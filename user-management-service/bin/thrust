#!/usr/bin/env ruby
instrumented = ENV.fetch("IAM_THRUSTER_BINARY", "/usr/local/bin/iam-thrust")
exec instrumented, *ARGV if File.executable?(instrumented)

require "rubygems"
require "bundler/setup"

load Gem.bin_path("thruster", "thrust")
